**0-sha1.sh** - A bash script that hashes a given password

**1-sha256.sh** - A bash script that hashes a given password

**2-md5.sh** - A bash script that hashes a given password

**3-password_hash.sh** - A bash script that accepts a password as argument $1, combines it with a random 16-character value using OpenSSL, and generate SHA-512 hash of the new password using OpenSSL

**4-wordlist_john.sh** - A bash script that crack the password using John the Ripper based on the file hash.txt

**5-windows_john.sh** - A bash script that crack the password using John the Ripper based on the file hash.txt

**6-crack_john.sh** - A bash scriptthat crack the password using John the Ripper based on the file crack.txt

**7-crack_hashcat.sh** - A bash scriptthat crack the password using hashcat based on the file hash.txt

**8-combination_hashcat.sh** - A bash scriptthat combine two wordlists using hashcat
